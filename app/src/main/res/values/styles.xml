<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--colorPrimary 应用的主要色调,actionBar默认使用该颜色-->
        <item name="colorPrimary">@color/colorwhite</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/welcome</item>
    </style>

    <!-- 去掉tab顶部的黑边 -->
    <style name="no_title" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 沉浸式状态栏 -->
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:clipToPadding">false</item>
        <item name="colorAccent">@color/font_text_blue</item>

        <item name="android:textColorPrimary">@color/login_reg_font</item>

        <!--自定义AlertDialog-->
        <!--<item name="alertDialogTheme">@style/Theme.AppCompat.Light.Dialog.Alert.Self</item>-->
    </style>


    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及透明程度-->
        <!--<item name="android:windowFrame">@null</item>-->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme_light" parent="AppTheme.Base">
        <!--toolbar标题文字颜色-->
        <item name="colorPrimary">@color/colorwhite</item>
        <item name="android:textColorPrimary">@android:color/black</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.NoActionBar"></style>


    <style name="Widget.GifView" parent="@android:style/Widget" />
    <!--首次进入无黑色-->
    <style name="welcomeTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <!--<item name="android:windowTranslucentStatus" tools:targetApi="19">true</item>-->
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/welcome</item>
    </style>

    <!--字体拼接-->
    <style name="fontstyle0">
        <item name="android:textSize">26px</item>
        <item name="android:textColor">#777777</item>
    </style>

    <style name="fontstyle2">
        <item name="android:textSize">23dip</item>
        <item name="android:textColor">#108ee9</item>
        <item name="android:textStyle">italic</item>
    </style>

    <!--分割线-->
    <style name="DividerHorizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1px</item>
        <item name="android:background">@color/login_reg_view_color</item>
    </style>


    <!--自定义CheckBox的样式-->
    <style name="CustomCheckboxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/tab_protocol_checkbox</item>
    </style>
    <!-- 标题栏 -->
    <style name="TitleStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">90px</item>
        <!--<item name="android:background">@color/black_deep</item>-->
        <item name="android:gravity">center</item>
        <!--<item name="android:textColor">@color/blue</item>-->
        <item name="android:textSize">20sp</item>
    </style>
</resources>
